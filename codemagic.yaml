workflows:
  flutter-ios-build:
    name: Flutter iOS Build
    max_build_duration: 60

    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main

    environment:
      flutter: stable
      xcode: latest
      cocoapods: default

    scripts:
      - name: Go to Flutter project
        script: |
          cd mobile_flutter

      - name: Get dependencies
        script: |
          flutter pub get

      - name: Install CocoaPods
        script: |
          cd ios
          pod install
          cd ..

      - name: Build IPA
        script: |
          flutter build ipa --release --no-codesign

    artifacts:
      - mobile_flutter/build/ios/ipa/*.ipa