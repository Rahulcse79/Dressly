# Prometheus configuration for Dressly
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'dressly-backend'
    static_configs:
      - targets: ['backend:8080']
    metrics_path: '/api/v1/health'
    scrape_interval: 10s
